# 数据库配置
DATABASE_URL=mysql+pymysql://root@localhost:3306/filemanager
DATABASE_PASSWORD=your_database_password_here

# JWT密钥（必须设置，用于token加密）
SECRET_KEY=generate_a_secure_random_string_here

# 调试模式（生产环境设为 False）
DEBUG=False

# 文件存储路径
STORAGE_PATH=./storage

# 存储配额（GB），默认 2
STORAGE_QUOTA_GB=2